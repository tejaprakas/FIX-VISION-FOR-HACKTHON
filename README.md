# FIX-VISION-FOR-HACKTHON
The project was built using Vite and converted into a production-ready version by generating optimized files in the `dist` folder. These build files were then deployed on a cloud hosting platform, making the application publicly accessible, fast, and scalable instead of being limited to localhost.
